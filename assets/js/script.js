const squaresContainer = document.querySelector(".squares");
const colors = ["blue", "yellow", "green", "red", "orange", "purple"];
const colorsPicklist = [...colors, ...colors]
const squareCount = colorsPicklist.length;

